# Previous runtime support

ppstack supported another agent runtime before the Pi-only migration. The last dual-runtime state is available in Git history. Local raw migration captures, when present, live under ignored `debug/traces/`.

Active ppstack skills now target Pi and the installed `pi-subagents` package only.
